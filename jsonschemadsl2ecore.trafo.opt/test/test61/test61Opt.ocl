-- OCL contraints for /Users/aless/git/jsonschemadsl.parent/jsonschemadsl2ecore.trafo.opt/input/test61
import 'http://jku.bise.at/test61'

package test61

	context test61::Test61RootAdditionalProperties 

	inv Test61RootAdditionalPropertiesValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootAdditionalProperties 

	inv Test61RootAdditionalPropertiesValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootProp1TypeInteger

	inv Test61RootProp1TypeIntegerIsInteger('The value of Test61RootProp1TypeInteger.prop1 must be integer'):
	if prop1.floor() = prop1
	then true
	else null
	endif

	context test61::Test61RootProp2

	inv Test61RootProp2IsInteger('The value of Test61RootProp2.prop2 must be integer'):
	if prop2.floor() = prop2
	then true
	else null
	endif

	context test61::Test61RootPatternProperties0 

	inv Test61RootPatternProperties0ValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties0 

	inv Test61RootPatternProperties0ValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties1 

	inv Test61RootPatternProperties1ValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties1 

	inv Test61RootPatternProperties1ValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties1

	inv Test61RootPatternProperties1IsInteger('The value of Test61RootPatternProperties1.patternProperties1 must be integer'):
	if patternProperties1.floor() = patternProperties1
	then true
	else null
	endif

	context test61::Test61RootPatternProperties2 

	inv Test61RootPatternProperties2ValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties2 

	inv Test61RootPatternProperties2ValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties0 

	inv Test61RootPatternProperties0patternProperties0Regex ('the key must match the regular expression ^N_'):
	if  self.find()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties1 

	inv Test61RootPatternProperties1patternProperties1Regex ('the key must match the regular expression ^I_'):
	if  self.find()
	then true
	else null
	endif

	context test61::Test61RootPatternProperties2 

	inv Test61RootPatternProperties2patternProperties2Regex ('the key must match the regular expression ^S_'):
	if  self.find()
	then true
	else null
	endif

endpackage

