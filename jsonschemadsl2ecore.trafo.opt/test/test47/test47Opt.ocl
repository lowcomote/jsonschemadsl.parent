-- OCL contraints for /Users/aless/git/jsonschemadsl.parent/jsonschemadsl2ecore.trafo.opt/input/test47
import 'http://jku.bise.at/test47'

package test47

	context test47::Test47RootAdditionalProperties 

	inv Test47RootAdditionalPropertiesValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test47::Test47RootAdditionalProperties 

	inv Test47RootAdditionalPropertiesValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test47::Test47Root 

	inv Test47RootDependenciesArrayprop1propA ('Test47Root requires that if property prop1 is present, also the property propA must exist'):
	if  not self.get('prop1').oclIsUndefined()
	then 
		if not self.get('propA').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	context test47::Test47Root 

	inv Test47RootDependenciesArrayprop1propB ('Test47Root requires that if property prop1 is present, also the property propB must exist'):
	if  not self.get('prop1').oclIsUndefined()
	then 
		if not self.get('propB').oclIsUndefined() then true
		else null
		endif				
	else true
	endif


	context test47::Test47Root 

	inv Test47RootDependenciesArrayprop2propC ('Test47Root requires that if property prop2 is present, also the property propC must exist'):
	if  not self.get('prop2').oclIsUndefined()
	then 
		if not self.get('propC').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	context test47::Test47Root 

	inv Test47RootDependenciesArrayprop2propD ('Test47Root requires that if property prop2 is present, also the property propD must exist'):
	if  not self.get('prop2').oclIsUndefined()
	then 
		if not self.get('propD').oclIsUndefined() then true
		else null
		endif				
	else true
	endif


endpackage

