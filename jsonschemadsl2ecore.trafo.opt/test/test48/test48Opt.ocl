-- OCL contraints for /Users/aless/git/jsonschemadsl.parent/jsonschemadsl2ecore.trafo.opt/input/test48
import 'http://jku.bise.at/test48'

package test48

	context test48::Test48RootAdditionalProperties 

	inv Test48RootAdditionalPropertiesValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootAdditionalProperties 

	inv Test48RootAdditionalPropertiesValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4AdditionalProperties 

	inv Test48RootProp4AdditionalPropertiesValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4AdditionalProperties 

	inv Test48RootProp4AdditionalPropertiesValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4PatternProperties0 

	inv Test48RootProp4PatternProperties0ValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4PatternProperties0 

	inv Test48RootProp4PatternProperties0ValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4PatternProperties1 

	inv Test48RootProp4PatternProperties1ValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4PatternProperties1 

	inv Test48RootProp4PatternProperties1ValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp6AdditionalProperties 

	inv Test48RootProp6AdditionalPropertiesValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp6AdditionalProperties 

	inv Test48RootProp6AdditionalPropertiesValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test48::Test48RootProp4PatternProperties0 

	inv Test48RootProp4PatternProperties0patternProperties0Regex ('the key must match the regular expression ^I'):
	if  self.find()
	then true
	else null
	endif

	context test48::Test48RootProp4PatternProperties1 

	inv Test48RootProp4PatternProperties1patternProperties1Regex ('the key must match the regular expression ^S'):
	if  self.find()
	then true
	else null
	endif

	context test48::Test48RootProp4 

	inv Test48RootProp4DependenciesArrayprop5Ingrid ('Test48RootProp4 requires that if property prop5 is present, also the property Ingrid must exist'):
	if  not self.get('prop5').oclIsUndefined()
	then 
		if not self.get('Ingrid').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	
	context test48::Test48RootProp4 

	inv Test48RootProp4DependenciesArrayprop5prop51 ('Test48RootProp4 requires that if property prop5 is present, also the property prop51 must exist'):
	if  not self.get('prop5').oclIsUndefined()
	then 
		if not self.get('prop51').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


	context test48::Test48RootProp4 

	inv Test48RootProp4DependenciesArrayIlandSardegna ('Test48RootProp4 requires that if property Iland is present, also the property Sardegna must exist'):
	if  not self.get('Iland').oclIsUndefined()
	then 
		if not self.get('Sardegna').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


	context test48::Test48RootProp4 

	inv Test48RootProp4DependenciesArrayprop51prop52 ('Test48RootProp4 requires that if property prop51 is present, also the property prop52 must exist'):
	if  not self.get('prop51').oclIsUndefined()
	then 
		if not self.get('prop52').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


	context test48::Test48RootProp6TypeObject 

	inv Test48RootProp6TypeObjectDependenciesArrayprop8prop8A ('Test48RootProp6TypeObject requires that if property prop8 is present, also the property prop8A must exist'):
	if  not self.get('prop8').oclIsUndefined()
	then 
		if not self.get('prop8A').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	
	context test48::Test48RootProp6TypeObject 

	inv Test48RootProp6TypeObjectDependenciesArrayprop8prop8B ('Test48RootProp6TypeObject requires that if property prop8 is present, also the property prop8B must exist'):
	if  not self.get('prop8').oclIsUndefined()
	then 
		if not self.get('prop8B').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


	context test48::Test48RootProp6TypeObject 

	inv Test48RootProp6TypeObjectDependenciesArrayprop9prop9C ('Test48RootProp6TypeObject requires that if property prop9 is present, also the property prop9C must exist'):
	if  not self.get('prop9').oclIsUndefined()
	then 
		if not self.get('prop9C').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	
	context test48::Test48RootProp6TypeObject 

	inv Test48RootProp6TypeObjectDependenciesArrayprop9prop9D ('Test48RootProp6TypeObject requires that if property prop9 is present, also the property prop9D must exist'):
	if  not self.get('prop9').oclIsUndefined()
	then 
		if not self.get('prop9D').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


	context test48::Test48Root 

	inv Test48RootDependenciesArrayprop1propA ('Test48Root requires that if property prop1 is present, also the property propA must exist'):
	if  not self.get('prop1').oclIsUndefined()
	then 
		if not self.get('propA').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	
	context test48::Test48Root 

	inv Test48RootDependenciesArrayprop1propB ('Test48Root requires that if property prop1 is present, also the property propB must exist'):
	if  not self.get('prop1').oclIsUndefined()
	then 
		if not self.get('propB').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


	context test48::Test48Root 

	inv Test48RootDependenciesArrayprop2propC ('Test48Root requires that if property prop2 is present, also the property propC must exist'):
	if  not self.get('prop2').oclIsUndefined()
	then 
		if not self.get('propC').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	
	context test48::Test48Root 

	inv Test48RootDependenciesArrayprop2propD ('Test48Root requires that if property prop2 is present, also the property propD must exist'):
	if  not self.get('prop2').oclIsUndefined()
	then 
		if not self.get('propD').oclIsUndefined() then true
		else null
		endif				
	else true
	endif
	


endpackage

