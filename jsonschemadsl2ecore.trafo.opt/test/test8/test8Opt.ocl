-- OCL contraints for /Users/aless/git/jsonschemadsl.parent/jsonschemadsl2ecore.trafo.opt/input/test8
import 'http://jku.bise.at/test8'

package test8

	context test8::Test8RootPatternProperties0 

	inv Test8RootPatternProperties0patternProperties0Regex ('the key must match the regular expression ^S_'):
	if  key.matches('^S_')
	then true
	else null
	endif

	context test8::Test8RootPatternProperties1 

	inv Test8RootPatternProperties1patternProperties1Regex ('the key must match the regular expression ^I_'):
	if  key.matches('^I_')
	then true
	else null
	endif

	context test8::Test8RootPatternProperties2 

	inv Test8RootPatternProperties2patternProperties2Regex ('the key must match the regular expression ^P_'):
	if  key.matches('^P_')
	then true
	else null
	endif

	context test8::Test8RootPatternProperties3 

	inv Test8RootPatternProperties3patternProperties3Regex ('the key must match the regular expression ^H_'):
	if  key.matches('^H_')
	then true
	else null
	endif

endpackage

