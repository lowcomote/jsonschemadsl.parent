-- OCL contraints for /Users/aless/git/jsonschemadsl.parent/jsonschemadsl2ecore.trafo.opt/input/test63
import 'http://jku.bise.at/test63'

package test63

	context test63::Test63RootAdditionalProperties 

	inv Test63RootAdditionalPropertiesValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test63::Test63RootAdditionalProperties 

	inv Test63RootAdditionalPropertiesValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test63::Test63RootProp1TypeInteger

	inv Test63RootProp1TypeIntegerIsInteger('The value of Test63RootProp1TypeInteger.prop1 must be integer'):
	if prop1.floor() = prop1
	then true
	else null
	endif

	context test63::Test63RootPatternProperties0 

	inv Test63RootPatternProperties0ValidatorOnProperties (' key can not be equals to a defined property'):
	if  self.validateKeyOnDefinedProperties().oclIsUndefined()
	then true
	else null
	endif

	context test63::Test63RootPatternProperties0 

	inv Test63RootPatternProperties0ValidatorOnPatternProperties (' key can not match the patterns of the defined pattern properties'):
	if  self.validateKeyOnDefinedPatternProperties().oclIsUndefined()
	then true
	else null
	endif

	context test63::Test63RootProp1TypeString

	inv Test63RootProp1Pattern('The value of Test63RootProp1TypeString must match the regular expression ^a*$'):
	if self.regexMatch()
	then true
	else null
	endif

	context test63::Test63RootProp2

	inv Test63RootProp2Pattern('The value of Test63RootProp2 must match the regular expression ^b*$'):
	if self.regexMatch()
	then true
	else null
	endif

	context test63::Test63RootPatternProperties0

	inv Test63RootPatternProperties0Pattern('The value of Test63RootPatternProperties0 must match the regular expression PP'):
	if self.regexMatch()
	then true
	else null
	endif

	context test63::Test63RootPatternProperties0 

	inv Test63RootPatternProperties0patternProperties0Regex ('the key must match the regular expression ^S_'):
	if  self.find()
	then true
	else null
	endif

endpackage

